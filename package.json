{
  "name": "digitivia-ai-agent",
  "version": "1.0.0",
  "description": "Digitivia AI Agent — AI-Powered Command Center",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "format": "prettier --write 'src/**/*.{js,css,json}'"
  },
  "devDependencies": {
    "vite": "^6.3.0",
    "eslint": "^9.14.0",
    "prettier": "^3.4.0"
  }
}
