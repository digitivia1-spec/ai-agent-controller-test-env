I want you to implement a production-ready Prompt Assistant feature for the existing AI agent interface.

This feature must exist across all AI agent tabs where the system prompt / role instructions editor exists.
Do not implement it for only one tab.
If the app has multiple AI agent tabs, prompt editors, or role-instruction areas, the feature must be designed as a reusable pattern and integrated consistently across all of them.

FULL ACCESS DATABASE CREDs
export SUPABASE_URL="https://xrycghxaxqzvkmzqzzkx.supabase.co"
export SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5lZXBnc3BwY2VtendhcGNleHlzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njc1NDI5NzEsImV4cCI6MjA4MzExODk3MX0.1gXea9dkLrE8CY80ho0wC0al04DCvMJK5UimqAj96e0"
export SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5lZXBnc3BwY2VtendhcGNleHlzIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NzU0Mjk3MSwiZXhwIjoyMDgzMTE4OTcxfQ.IgnOZVsfjk-b9RsAqI-1LwhxVACr1SZsQ0YlXQz-jt4"

PAT : "sbp_0909544731e3861eccfced416512eb5968207c82"

Critical architecture rule:
The workflow is not the source of truth for the app.
The workflow is only a backend generation endpoint that receives structured input and returns structured output.
It should be treated as a stateless or near-stateless prompt-generation service.
Do not move core product rules, ownership validation, persistence logic, plan enforcement authority, or database orchestration into the workflow unless absolutely necessary.
Those responsibilities must be handled primarily from the UI / website / application code side and the real database side.

Important:
Before implementing UI wiring, inspect the real Supabase database schema and the real app codebase first.
Do not guess table names, column names, relations, plan fields, usage fields, prompt fields, agent tabs, or ownership rules.
Use the actual live schema and actual app structure.
You have full approved access to inspect the Supabase project and should use that access before implementing.
The final implementation must match the real schema exactly, not an assumed schema.

Feature goal:
Help the user do one of these with minimal effort:
1) improve the current prompt
2) create a new prompt
3) keep the current prompt as-is

UI placement:
This feature must live inside the existing system prompt / role instructions area.
Do not build it as a separate page.
Do not make it feel like another product.
It must feel like a natural extension of the existing prompt editor.

Important correction:
Do not show a channel chooser.
The user should not be asked to choose WhatsApp, Messenger, Instagram, or any other channel inside this assistant.
This feature already runs inside the current AI agent / tab / context.
The page context already knows where the user is.

Scope:
- all AI agent tabs
- all prompt editors of this type
- shared reusable UI logic
- shared reusable request/response handling
- shared responsive behavior
- shared RTL/LTR support

Design and UX requirements:
- mobile-first
- mobile-responsive
- RTL and LTR support
- Arabic and English support
- dark dashboard style consistency
- lightweight, premium, low-friction UX
- result must appear immediately in the same prompt editor area

Core UI behavior:
Add a compact Prompt Assistant bar above the existing prompt editor.
Do not replace the editor.
Keep the prompt editor as the center of gravity.

Primary actions:
- Improve current prompt
- Create new prompt
- Use current prompt

Interaction model:
- mobile: bottom sheet
- desktop: side drawer or compact side panel
- after generation, show the returned prompt draft immediately in the same editor area
- do not send the user to a different page
- do not rely on popup-only results
- do not force manual copy every time

Editor states:
1) normal state
2) loading state
3) draft state
4) applied state
5) error state

When a draft exists:
- desktop: clean current vs suggested comparison
- mobile: tabs or stacked cards for current / suggested
- actions:
  - Apply
  - Replace current
  - Copy
  - Regenerate
  - Cancel

If the prompt is locked:
- user can still generate a draft
- user cannot silently overwrite
- locked state must block direct overwrite only
- locked state must not block trying the assistant

Data collection must be simple and direct.
Do not turn this into a long briefing form.

Required fields:
- What should this agent do?
- What is your business?
- How should it speak?
- What language should it use?

Optional fields:
- What information should it collect?
- What should it avoid?
- Important note

Do not include channel selection.

Preferred input style:
- chips / pills / segmented controls where possible
- minimal typing
- one short business field
- one optional small textarea

Suggested goal options:
- Answer customers
- Sell / convert leads
- Collect customer info
- Book appointments
- Reply on social media
- General assistant

Suggested tone options:
- Professional
- Friendly
- Sales-focused
- Short and direct
- Premium

Suggested language options:
- Arabic
- English
- Same as customer
- Arabic + English

Suggested collect options:
- Name
- Phone
- Location
- Budget
- Service needed
- Preferred time

Suggested avoid options:
- No prices unless asked
- No promises
- No long replies
- No discounts without approval
- No medical/legal advice

Very important production rule:
The workflow must only receive clean structured data and return clean structured data.
Everything else that makes the feature truly production-ready should be handled from the code side and database side.

That means the UI / website / app code must own these responsibilities:

1) Real schema inspection and mapping
- inspect the real Supabase schema first
- identify the real tables/columns used for AI agent tabs, prompt storage, plans, usage, memberships, and ownership
- map the feature to real existing entities
- do not invent or assume schema names

2) Source of truth for plan and usage
- the workflow must not be trusted as the source of truth for plan limits
- the app/database side must determine the user plan and current usage from real stored data
- only send the workflow the already validated/canonical plan context needed for generation behavior if needed
- plan enforcement authority must live on the real app/database side

Plan limits to support:
- starter: 15 requests per day
- growth: 25 requests per day
- pro: 40 requests per day

3) Usage counting and daily quota enforcement
- usage must be counted on the real app/database side
- quota checks must be driven by trusted stored values
- avoid relying on client-only counters
- avoid trusting editable frontend state
- if atomic or transactional updates are needed, do them on the code/database side
- the workflow should not be the primary quota ledger

4) Ownership and permission validation
- validate workspace/tenant/user ownership on the app side before calling the workflow
- validate that the current user can access the current agent/tab/prompt
- validate that the current prompt row/cell belongs to the correct workspace/user
- do not trust raw browser context blindly

5) Save/apply behavior
- the workflow can generate the draft
- the app code must own applying/saving the chosen result to the real database
- on Apply or Replace, update the correct real prompt field/row/column from the code side
- do not depend on the workflow to persist the final prompt unless there is a deliberate secure backend save layer

6) Request contract normalization
- the UI/code side must always send a stable canonical payload
- field names must be normalized before calling the workflow
- if the database stores the prompt in one row, one column, one JSON field, or one tab-specific config cell, normalize that cleanly before sending
- workflow input should be clean and consistent, not a dump of random UI state

7) Response application
- the workflow returns structured output only
- the UI/code side decides how to display it, compare it, apply it, copy it, cancel it, or save it
- the same response renderer should work across all AI agent tabs

8) Secure endpoint calling
- webhook URL configuration, auth headers, environment variables, retry policy, and error handling must be handled from the app side
- do not treat the webhook as a public unprotected business-rule engine
- secure the integration from the application side appropriately

9) Exact compatibility with all AI agent tabs
- some tabs may store prompt state differently
- inspect and unify the UI integration layer so the assistant works consistently even if the underlying tab data model differs
- create a reusable adapter layer if needed

10) Production readiness expectations
- no guessed table names
- no guessed tab keys
- no guessed plan fields
- no guessed prompt cells
- no silent overwrites
- no client-only quota truth
- no inconsistent request shapes between tabs

Workflow role only:
The workflow is only responsible for:
- receiving the structured request
- processing mode:
  - improve_existing
  - create_new
  - use_current
- generating the prompt result
- returning structured response data

The workflow is not responsible for being the primary system of record.
The application code and database layer are responsible for production truth.

Recommended request shape sent from the app to the workflow:
- mode
- originalPrompt
- goal
- business
- tone
- language
- collectFields
- avoidRules
- importantNote
- locale
- textDirection
- workspaceId
- agentId
- agentTabId
- agentTabKey
- currentPromptValue
- promptStorageType
- promptRecordId
- promptColumnKey
- planKey
- validatedDailyLimit
- validatedDailyUsed
- remainingToday

Only include fields that are real and verified from the actual database/app structure.
Do not fake them.

Recommended response shape from workflow back to UI:
- success
- modeApplied
- originalPrompt
- finalPrompt
- summary
- missingFields
- questions
- canApply
- canCopy
- canRegenerate

Behavior rules:
- improve_existing: improve the current prompt while preserving user intent
- create_new: generate a new prompt from the simple collected inputs
- use_current: do not generate a new prompt, keep the current content
- preserve the user language unless explicitly changed
- support Arabic and English correctly
- display RTL/LTR correctly

Here is the workflow JSON as i need everything consistent and harmonic with it as it is: 

{
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "prompt-imporve",
        "responseMode": "responseNode",
        "options": {
          "allowedOrigins": "*"
        }
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        -528,
        36
      ],
      "id": "a98aa59c-93e0-4f08-b352-f05a6a2302bd",
      "name": "Webhook",
      "webhookId": "bf8a46bc-8775-4bc1-951b-908f40eed6a4"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const body = $json.body ?? $json;\nconst parseArray = (value) => {\n  if (Array.isArray(value)) return value.filter(Boolean).map((v) => String(v).trim()).filter(Boolean);\n  if (typeof value === 'string') {\n    const trimmed = value.trim();\n    if (!trimmed) return [];\n    try {\n      const parsed = JSON.parse(trimmed);\n      if (Array.isArray(parsed)) return parsed.filter(Boolean).map((v) => String(v).trim()).filter(Boolean);\n    } catch (_) {}\n    return trimmed.split(',').map((v) => v.trim()).filter(Boolean);\n  }\n  return [];\n};\nconst firstNonEmpty = (...values) => {\n  for (const value of values) {\n    if (value === undefined || value === null) continue;\n    if (typeof value === 'string' && value.trim() === '') continue;\n    return value;\n  }\n  return null;\n};\nconst toInt = (value, fallback = null) => {\n  if (value === null || value === undefined || value === '') return fallback;\n  const num = Number(value);\n  return Number.isFinite(num) ? Math.trunc(num) : fallback;\n};\nconst normalizePlanKey = (value) => {\n  const raw = String(value || '').trim().toLowerCase();\n  if (!raw) return 'starter';\n  if (['starter', 'start', 'basic'].includes(raw)) return 'starter';\n  if (['growth', 'grow'].includes(raw)) return 'growth';\n  if (['pro', 'professional'].includes(raw)) return 'pro';\n  return raw;\n};\nconst planLimits = { starter: 15, growth: 25, pro: 40 };\nconst directionFromLocale = (locale) => String(locale || '').toLowerCase().startsWith('ar') ? 'rtl' : 'ltr';\nconst rawLocale = String(firstNonEmpty(body.locale, body.language, body.lang, body.userLanguage, 'en') || 'en').trim();\nconst locale = rawLocale.toLowerCase().startsWith('ar') ? 'ar' : 'en';\nconst textDirection = String(firstNonEmpty(body.textDirection, body.direction, directionFromLocale(locale)) || directionFromLocale(locale)).toLowerCase() === 'rtl' ? 'rtl' : 'ltr';\n\nconst context = body.context && typeof body.context === 'object' ? body.context : {};\nconst dbContext = body.dbContext && typeof body.dbContext === 'object' ? body.dbContext : {};\nconst dbPlan = body.plan && typeof body.plan === 'object' ? body.plan : {};\n\nconst modeRaw = String(firstNonEmpty(body.mode, body.action, '') || '').trim().toLowerCase();\nconst mode = ['improve_existing', 'create_new', 'use_current'].includes(modeRaw) ? modeRaw : '';\n\nconst originalPrompt = String(firstNonEmpty(\n  body.originalPrompt,\n  body.original_prompt,\n  dbContext.promptCellValue,\n  body.currentPrompt,\n  ''\n) || '').trim();\n\nconst goal = String(firstNonEmpty(body.goal, body.agentGoal, body.whatShouldThisAgentDo, '') || '').trim();\nconst business = String(firstNonEmpty(body.business, body.businessType, body.businessDescription, '') || '').trim();\nconst tone = String(firstNonEmpty(body.tone, body.style, body.voice, '') || '').trim();\nconst language = String(firstNonEmpty(body.languagePreference, body.languageChoice, body.promptLanguage, body.language, locale) || locale).trim();\nconst collectFields = parseArray(firstNonEmpty(body.collectFields, body.collect, body.collect_fields, []));\nconst avoidRules = parseArray(firstNonEmpty(body.avoidRules, body.avoid, body.avoid_rules, []));\nconst importantNote = String(firstNonEmpty(body.importantNote, body.note, body.notes, '') || '').trim();\n\nconst planKey = normalizePlanKey(firstNonEmpty(\n  dbPlan.key,\n  body.planKey,\n  body.plan_key,\n  dbContext.planKey,\n  dbContext.subscriptionPlan,\n  'starter'\n));\nconst dailyLimit = toInt(firstNonEmpty(dbPlan.dailyLimit, body.dailyLimit, body.daily_limit, dbContext.dailyLimit, planLimits[planKey]), planLimits[planKey] ?? 15);\nconst dailyUsed = Math.max(0, toInt(firstNonEmpty(dbPlan.dailyUsed, body.dailyUsed, body.daily_used, dbContext.dailyUsed, dbContext.promptAssistantDailyUsed, 0), 0));\nconst dailyRemaining = Math.max(dailyLimit - dailyUsed, 0);\nconst isBlocked = dailyUsed >= dailyLimit;\n\nconst workspaceId = firstNonEmpty(context.workspaceId, body.workspaceId, body.workspace_id, dbContext.workspaceId);\nconst agentId = firstNonEmpty(context.agentId, body.agentId, body.agent_id, dbContext.agentId);\nconst agentTabId = firstNonEmpty(context.agentTabId, body.agentTabId, body.agent_tab_id, dbContext.agentTabId);\nconst agentTabKey = firstNonEmpty(context.agentTabKey, body.agentTabKey, body.agent_tab_key, dbContext.agentTabKey);\nconst agentTabLabel = firstNonEmpty(context.agentTabLabel, body.agentTabLabel, body.agent_tab_label, dbContext.agentTabLabel);\n\nlet route = 'generate';\nlet errorCode = null;\nlet errorMessage = null;\n\nif (!mode) {\n  route = 'direct';\n  errorCode = 'missing_mode';\n  errorMessage = 'mode must be one of improve_existing, create_new, or use_current';\n} else if (mode === 'improve_existing' && !originalPrompt) {\n  route = 'direct';\n  errorCode = 'missing_original_prompt';\n  errorMessage = 'originalPrompt is required for improve_existing';\n} else if (mode === 'use_current') {\n  route = 'direct';\n} else if (isBlocked) {\n  route = 'direct';\n  errorCode = 'daily_limit_reached';\n  errorMessage = 'Daily prompt assistant limit reached for this plan';\n}\n\nconst missingFields = [];\nif (mode === 'create_new') {\n  if (!goal) missingFields.push('goal');\n  if (!business) missingFields.push('business');\n  if (!tone) missingFields.push('tone');\n  if (!language) missingFields.push('language');\n}\n\nconst requestId = (globalThis.crypto?.randomUUID?.() ?? `req_${Date.now()}_${Math.random().toString(36).slice(2, 10)}`);\n\nreturn {\n  json: {\n    requestId,\n    receivedAt: new Date().toISOString(),\n    mode,\n    route,\n    errorCode,\n    errorMessage,\n    locale,\n    textDirection,\n    originalPrompt,\n    goal,\n    business,\n    tone,\n    language,\n    collectFields,\n    avoidRules,\n    importantNote,\n    missingFields,\n    planKey,\n    dailyLimit,\n    dailyUsed,\n    dailyRemaining,\n    isBlocked,\n    context: {\n      workspaceId,\n      agentId,\n      agentTabId,\n      agentTabKey,\n      agentTabLabel,\n    },\n    dbContext: {\n      promptSource: firstNonEmpty(dbContext.promptSource, body.promptSource),\n      promptRowId: firstNonEmpty(dbContext.promptRowId, body.promptRowId),\n      promptColumn: firstNonEmpty(dbContext.promptColumn, body.promptColumn),\n      promptCellValue: firstNonEmpty(dbContext.promptCellValue, body.promptCellValue, originalPrompt),\n    }\n  }\n};"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -304,
        36
      ],
      "id": "e6f6d374-a84a-4d26-9e30-d4652eeced5d",
      "name": "Normalize Request"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "f1a1476f-30aa-43f0-bd20-7e76833936ee",
              "leftValue": "={{$json.route}}",
              "rightValue": "generate",
              "operator": {
                "type": "string",
                "operation": "equals",
                "name": "filter.operator.equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        -80,
        36
      ],
      "id": "fcdad1f1-1ecc-480b-b0ca-09cf76f82850",
      "name": "IF Generate"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const base = $json;\nconst limit = {\n  planKey: base.planKey,\n  dailyLimit: base.dailyLimit,\n  dailyUsed: base.dailyUsed,\n  dailyRemaining: base.dailyRemaining,\n  blocked: base.isBlocked,\n};\n\nif (base.errorCode === 'missing_mode' || base.errorCode === 'missing_original_prompt') {\n  return {\n    json: {\n      success: false,\n      status: 'error',\n      modeApplied: base.mode || null,\n      errorCode: base.errorCode,\n      message: base.errorMessage,\n      originalPrompt: base.originalPrompt || '',\n      finalPrompt: null,\n      summary: null,\n      missingFields: base.missingFields || [],\n      questions: [],\n      canApply: false,\n      canCopy: false,\n      canRegenerate: false,\n      locale: base.locale,\n      textDirection: base.textDirection,\n      limit,\n      requestId: base.requestId,\n    }\n  };\n}\n\nif (base.mode === 'use_current') {\n  return {\n    json: {\n      success: true,\n      status: 'ok',\n      modeApplied: 'use_current',\n      originalPrompt: base.originalPrompt || '',\n      finalPrompt: base.originalPrompt || '',\n      summary: base.locale === 'ar' ? 'تم الاحتفاظ بالبرومبت الحالي بدون تعديل.' : 'The current prompt was kept without changes.',\n      missingFields: [],\n      questions: [],\n      canApply: false,\n      canCopy: true,\n      canRegenerate: false,\n      locale: base.locale,\n      textDirection: base.textDirection,\n      limit,\n      requestId: base.requestId,\n      context: base.context,\n      dbContext: base.dbContext,\n    }\n  };\n}\n\nreturn {\n  json: {\n    success: false,\n    status: 'limit_exceeded',\n    modeApplied: base.mode,\n    errorCode: base.errorCode || 'daily_limit_reached',\n    message: base.errorMessage || 'Daily prompt assistant limit reached',\n    originalPrompt: base.originalPrompt || '',\n    finalPrompt: null,\n    summary: null,\n    missingFields: [],\n    questions: [],\n    canApply: false,\n    canCopy: false,\n    canRegenerate: false,\n    locale: base.locale,\n    textDirection: base.textDirection,\n    limit,\n    requestId: base.requestId,\n    context: base.context,\n    dbContext: base.dbContext,\n  }\n};"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        144,
        -112
      ],
      "id": "7d5b38bc-962d-459d-870f-b57ef9023d51",
      "name": "Build Direct Response"
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{$json}}",
        "options": {
          "responseCode": "={{$json.success ? 200 : ($json.status === 'limit_exceeded' ? 429 : 400)}}"
        }
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.5,
      "position": [
        432,
        -112
      ],
      "id": "70a20044-ceaf-4a88-b4ae-1881d73b15c0",
      "name": "Respond Direct"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const pickLanguageInstruction = (value) => {\n  const raw = String(value || '').toLowerCase();\n  if (raw.includes('arabic') || raw.includes('ar') || raw.includes('عربي')) return 'Arabic';\n  if (raw.includes('english') || raw.includes('en')) return 'English';\n  if (raw.includes('same')) return 'Same as the customer/user input language';\n  if (raw.includes('+')) return 'Arabic and English when appropriate';\n  return value || 'Preserve the user language';\n};\n\nconst chatInput = [\n  `Mode: ${$json.mode}`,\n  `Locale: ${$json.locale}`,\n  `Text direction: ${$json.textDirection}`,\n  `Plan: ${$json.planKey}`,\n  `Workspace ID: ${$json.context?.workspaceId || ''}`,\n  `Agent ID: ${$json.context?.agentId || ''}`,\n  `Agent Tab ID: ${$json.context?.agentTabId || ''}`,\n  `Agent Tab Key: ${$json.context?.agentTabKey || ''}`,\n  `Agent Tab Label: ${$json.context?.agentTabLabel || ''}`,\n  `Original prompt:`,\n  $json.originalPrompt || '',\n  ``,\n  `Goal: ${$json.goal || ''}`,\n  `Business: ${$json.business || ''}`,\n  `Tone: ${$json.tone || ''}`,\n  `Language preference: ${pickLanguageInstruction($json.language)}`,\n  `Collect fields: ${(Array.isArray($json.collectFields) ? $json.collectFields.join(', ') : '') || 'None'}`,\n  `Avoid rules: ${(Array.isArray($json.avoidRules) ? $json.avoidRules.join(', ') : '') || 'None'}`,\n  `Important note: ${$json.importantNote || ''}`,\n  `Missing fields: ${(Array.isArray($json.missingFields) ? $json.missingFields.join(', ') : '') || 'None'}`,\n  ``,\n  `Return strict JSON only.`\n].join('\\n');\n\nreturn {\n  json: {\n    ...$json,\n    chatInput,\n  }\n};"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        144,
        184
      ],
      "id": "80935c2b-d7c4-44bd-b71d-5ce57b5b12af",
      "name": "Prepare Chat Input"
    },
    {
      "parameters": {
        "promptType": "define",
        "text": "={{$json.chatInput}}",
        "options": {
          "systemMessage": "You are a Prompt Assistant backend generator for an app with AI agent tabs.\n\nYou improve prompts or create new prompts for agent system instructions.\n\nImportant behavior:\n- Return STRICT JSON only\n- No markdown\n- No code fences\n- No explanation outside JSON\n- Preserve the user's intended language unless another language is explicitly requested\n- Support Arabic and English correctly\n- Keep the result practical, production-ready, and directly usable as a system prompt\n- Do not invent business facts\n- If information is missing, still generate the best possible prompt and include concise follow-up questions\n\nYour JSON schema:\n{\n  \"finalPrompt\": \"string\",\n  \"summary\": \"string\",\n  \"questions\": [\"string\"],\n  \"missingFields\": [\"string\"]\n}\n\nGeneration rules:\n- For improve_existing: preserve intent, improve clarity, structure, scope, boundaries, tone, and usefulness\n- For create_new: build a clean, strong system prompt from the provided fields\n- Keep prompts clear, well-structured, and not bloated\n- If collect fields exist, include them naturally in the prompt\n- If avoid rules exist, include them as constraints\n- If missing fields exist, add 1 to 4 short direct questions only\n- summary must be short and useful for UI display\n- finalPrompt must be the full usable prompt text only"
        }
      },
      "type": "@n8n/n8n-nodes-langchain.agent",
      "typeVersion": 3.1,
      "position": [
        368,
        184
      ],
      "id": "70c56ee5-0c24-4924-8755-cb551ff2a7c2",
      "name": "AI Agent"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const base = $('Prepare Chat Input').item.json;\nconst raw = String($json.output ?? $json.text ?? $json.response ?? '').trim();\n\nlet parsed = null;\ntry {\n  parsed = JSON.parse(raw);\n} catch (_) {\n  const match = raw.match(/\\{[\\s\\S]*\\}/);\n  if (match) {\n    try {\n      parsed = JSON.parse(match[0]);\n    } catch (_) {}\n  }\n}\n\nconst finalPrompt = String(parsed?.finalPrompt || base.originalPrompt || '').trim();\nconst summary = String(parsed?.summary || '').trim() || (base.locale === 'ar'\n  ? 'تم إنشاء مسودة برومبت جديدة جاهزة للمراجعة.'\n  : 'A new prompt draft was generated and is ready for review.');\n\nconst questions = Array.isArray(parsed?.questions)\n  ? parsed.questions.map((q) => String(q).trim()).filter(Boolean).slice(0, 4)\n  : [];\n\nconst aiMissing = Array.isArray(parsed?.missingFields)\n  ? parsed.missingFields.map((q) => String(q).trim()).filter(Boolean)\n  : [];\n\nreturn {\n  json: {\n    success: true,\n    status: 'ok',\n    modeApplied: base.mode,\n    originalPrompt: base.originalPrompt || '',\n    finalPrompt,\n    summary,\n    missingFields: Array.from(new Set([...(base.missingFields || []), ...aiMissing])),\n    questions,\n    canApply: true,\n    canCopy: !!finalPrompt,\n    canRegenerate: true,\n    locale: base.locale,\n    textDirection: base.textDirection,\n    limit: {\n      planKey: base.planKey,\n      dailyLimit: base.dailyLimit,\n      dailyUsed: base.dailyUsed,\n      dailyRemaining: Math.max(base.dailyRemaining - 1, 0),\n      blocked: false,\n    },\n    requestId: base.requestId,\n    context: base.context,\n    dbContext: base.dbContext,\n    workflowVersion: 'prompt-assistant-v1'\n  }\n};"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        720,
        184
      ],
      "id": "7fb2b756-ec89-4123-a978-3af4c0efd30a",
      "name": "Format AI Response"
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{$json}}",
        "options": {
          "responseCode": 200
        }
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.5,
      "position": [
        944,
        184
      ],
      "id": "968bc30d-60b0-40fb-9f58-53cf14c940e8",
      "name": "Respond Success"
    },
    {
      "parameters": {
        "model": {
          "__rl": true,
          "mode": "list",
          "value": "gpt-5-mini"
        },
        "builtInTools": {},
        "options": {}
      },
      "type": "@n8n/n8n-nodes-langchain.lmChatOpenAi",
      "typeVersion": 1.3,
      "position": [
        376,
        408
      ],
      "id": "6f227bd8-912f-4c31-986f-597aeca61408",
      "name": "OpenAI Chat Model",
      "credentials": {
        "openAiApi": {
          "id": "VQo2qK7ftJSLNlmA",
          "name": "Digitivia API"
        }
      }
    },
    {
      "parameters": {
        "sessionIdType": "customKey"
      },
      "type": "@n8n/n8n-nodes-langchain.memoryRedisChat",
      "typeVersion": 1.5,
      "position": [
        504,
        408
      ],
      "id": "f428f617-3d25-47b3-a70f-f784097ecc13",
      "name": "Redis Chat Memory",
      "credentials": {
        "redis": {
          "id": "Rhl7MCZMjBgdGVWC",
          "name": "Redis account"
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Normalize Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize Request": {
      "main": [
        [
          {
            "node": "IF Generate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF Generate": {
      "main": [
        [
          {
            "node": "Prepare Chat Input",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Build Direct Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Direct Response": {
      "main": [
        [
          {
            "node": "Respond Direct",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Chat Input": {
      "main": [
        [
          {
            "node": "AI Agent",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AI Agent": {
      "main": [
        [
          {
            "node": "Format AI Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format AI Response": {
      "main": [
        [
          {
            "node": "Respond Success",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OpenAI Chat Model": {
      "ai_languageModel": [
        [
          {
            "node": "AI Agent",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    },
    "Redis Chat Memory": {
      "ai_memory": [
        [
          {
            "node": "AI Agent",
            "type": "ai_memory",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "fe981de90885f0d8a56f8e4f927b3ee33d1f994a1f11c74c559401477c4f2136"
  }
}

Also feel free to tell me what requires change in it.

Important UX rule:
Generate early, refine later.
Ask only the simplest useful fields first.
After a first result is shown, let the user refine it instead of forcing a long form.

Acceptance criteria:
- feature exists across all AI agent tabs where relevant
- channel chooser does not exist
- Supabase schema is inspected first before implementation
- prompt assistant is embedded in the current prompt card
- mobile-first layout works cleanly
- desktop responsive layout works cleanly
- Arabic RTL and English LTR both work correctly
- workflow is treated as receive/respond only
- source of truth for plans/usage/ownership/save behavior lives on the app/database side
- plan limits are implemented from trusted code/database logic:
  - starter 15/day
  - growth 25/day
  - pro 40/day
- UI sends clean canonical structured data to workflow
- workflow response is rendered immediately in the same prompt editor area
- user can apply, replace, copy, regenerate, or cancel
- locked prompts allow draft generation but do not allow silent overwrite
- implementation is production-ready and avoids schema assumptions